Document for git basic tutorial

git --version

git config --global user.name
git config --global user.email

git clone https://github.com/HUHUJUNG/i.teach.me.git

cd directory
cd ~//

git add file
git commit -m "message[file]"
git push

#버전관리 폴더에서 버전관리 프로젝트로 초기 생성
git init